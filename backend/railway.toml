[build]
dockerfilePath = "Dockerfile"
dockerBuildTarget = "production"

[deploy]
startCommand = "/bin/sh -c \"gunicorn app.main:app -k uvicorn.workers.UvicornWorker -w 2 --bind 0.0.0.0:$PORT\""
healthcheckPath = "/health"
restartPolicyType = "on_failure"
